type GenericFood {
  food_name: String!
  group: String
  sub_group: String
  scientific_name: String
}

type Query {
  getGenericFood(food_name: String!): GenericFood
  listGenericFoods: [GenericFood]
}

schema {
  query: Query
}
